# Orchestration Log — Bach spawn (2026-05-29T17:23:00Z)

**Agent:** Bach (general-purpose, claude-sonnet-4.6, background)  
**Session:** bach-5 retry (prior bath-4 hit platform 401)  
**Scope:** Seed-manifest ADR synthesis & shipping

---

## What happened

Daniel greenlit the seed-manifest design synthesized from the four-lens debate (Bach/Mahler/Mozart/Sibelius consensus). Bach drafted the ADR encapsulating all ten encoded decisions and five open questions.

## Output artifacts

**Shipped ADR:**
- `polyphony/docs/decisions/seed-manifest-as-durable-state.md` (~30KB, new ADR)

**Handoff document:**
- `.squad/decisions/inbox/bach-seed-manifest-adr-shipped.md` (notification to Scribe)

## Decisions encoded in ADR

1. Seed manifest as durable JSON at `.polyphony/state/{rootId}/seed-manifest.json`
2. Atomic `.tmp`+rename writes
3. `polyphony:plan-child-id` marker as canonical cross-run identity primitive
4. `plan_generation` as rich linked-chain object with `introduced_in` for orphan detection
5. Verb evolution on `write-plan` and `seed-children` only (no new verbs)
6. Script-internal retry: 3 attempts on codes 3 & 5; code 1 permanent
7. `seeding_blocked` terminal distinct from `workflow_abandoned`
8. GitHub explicitly out of scope; no `platform` field
9. Marker not WIQL-queryable (acceptable cost)
10. Orphan disposition workflow open

## Open questions documented

- Orphan disposition workflow (Sibelius/Wagner)
- `seeding_blocked` re-trigger discriminator in `root-item-dispatch.yaml` (Wagner)
- Possible top-level `schema_version` field
- Seeder idempotency across frequent renegotiation
- Registration of `seeding_complete` in domain-signal-envelope

## Cross-seam coordination tags

- **Sibelius:** marker embed atomicity (twig seam)
- **Wagner:** orphan renegotiation routing
- **Mahler:** script-level retry circuit and terminal routing
- **Mozart:** error-code clarifications (amendments to verb-error-boundary ADR)

## Status

✅ ADR shipped. 10 encoded decisions + 5 open questions documented. Ready for implementation sequencing.

---

**Filed:** 2026-05-29T17:23:00Z
